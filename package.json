{
  "name": "kantan-panel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "POC control panel: one-click kantan-hp site provisioning (GitHub + Cloudflare)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "tweetsodium": "^0.0.5"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
